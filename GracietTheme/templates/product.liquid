{%comment%} Fiche Produit {%endcomment%}
<div class="ficheProduct">
  {% assign current_variant = product.selected_or_first_available_variant %}
  {% assign featured_image = current_variant.featured_image | default: product.featured_image %}

  {%comment%} Zone sur les images {%endcomment%}
  <div class="ficheProduct-Img">
    <img src="{{ featured_image | img_url: 'large' }}" alt="{{ featured_image.alt | escape }}" id="ProductPhotoImg">
    <div class="ficheProduct-SousImg">
      {% for image in product.images %}
      <a href="{{ image.src | img_url: 'large' }}">
        <img src="{{ image.src | img_url: 'compact' }}" alt="{{ image.alt | escape }}">
      </a>
      {% endfor %}
    </div>
  </div>

  {%comment%} Zone sur le contenu de la fiche produit {%endcomment%}
  <div class="ficheProduct-Contenu">
    {%comment%} Zone sur le titre {%endcomment%}
    <h1>{{ product.title }}</h1>

    {%comment%} Zone sur la description du produit {%endcomment%}
    <div>{{ product.description }}</div>

    <form action="/cart/add" method="post" enctype="multipart/form-data"
      id="AddToCartForm">
      <select name="id" id="productSelect">
        {% for variant in product.variants %}
        {% if variant.available %}
        <option value="{{ variant.id }}">
          {{ variant.title }} - {{ variant.price | money_with_currency }}
        </option>
        {% else %}
        <option disabled="disabled">
          {{ variant.title }} - sold out
        </option>
        {% endif %}
        {% endfor %}
      </select>
      {{ current_variant.price | money }}
      <label for="Quantity">quantity</label>
      <input type="number" id="Quantity" name="quantity" value="1" min="1">
      <button type="submit" name="add" id="AddToCart">Add to cart</button>
    </form>

    {%comment%} Zone sur le prix {%endcomment%}
    {%comment%} Zone du boutton ajouter au panier {%endcomment%}

    {%comment%} Zone des logos Facebook/Twitter/Instagram {%endcomment%}
    <div class="ficheProduct-Logos">
      <button> <a href="https://www.facebook.com/sharer/sharer.php?u=https%3A%2F%2Fsylvie-strohl.com%2Fproduit%2Folivia-90%2F">
        <i class="fab fa-facebook"></i>
      </a></button>
    
      <button><a href="http://twitter.com/share?text=http://OLIVIA%2090&url=https%3A%2F%2Fsylvie-strohl.com%2Fproduit%2Folivia-90%2F"> 
        <i class="fab fa-twitter"></i>
      </a></button>
    
      <button> <a href="http://pinterest.com/pin/create/button/?url=https%3A%2F%2Fsylvie-strohl.com%2Fproduit%2Folivia-90%2F&media=https://sylvie-strohl.com/wp-content/uploads/2019/12/OLIVIA-90-FACE-SANS-M2TAL.jpg&description=http://OLIVIA%2090">
        <i class="fab fa-instagram"></i>
      </a></button>
    </div>
    {%comment%} Zone des produit associés {%endcomment%}
    {%comment%} Zone des bouttons pour changer d'articles {%endcomment%}
    <div class="ficheProduct-OtherArticle">
      <button><a href="">
        <i class="fas fa-chevron-right"></i>
      </a></button>

      <button><a href="">
        <i class="fas fa-chevron-left"></i>
      </a></button>
    </div>
  </div>
  
  
</div>





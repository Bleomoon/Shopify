<h1>{{ page.title }}</h1>
<div> {{ page.content }} </div>

<div>
  {% for product_type in shop.types %}
    {{ product_type | link_to_type }}<br>
  {% endfor %}
  
  {% for product in shop %}
    <p>{{ product.title }}</p>
  {%endfor%}
</div>

<h1>{{ page.title }}</h1>
<div> {{ page.content }} </div>

<div>
  {% for collection in collections %}
  {% for product in collection.products %}
    <p>{{ product.type }}<p>
    <p>{{ product.title }}<p>
  {% endfor %}
  {% endfor %}
</div>
